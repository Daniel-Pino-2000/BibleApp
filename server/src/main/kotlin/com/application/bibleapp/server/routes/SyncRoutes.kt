package com.application.bibleapp.server.routes

import io.ktor.server.routing.Route

/**
 * TODO: register the cross-device sync endpoints here (bookmarks, reading
 * progress, notes) once their request/response contract is designed.
 * See docs/API_Integration_Guide.pdf.
 */
fun Route.syncRoutes() {
}
