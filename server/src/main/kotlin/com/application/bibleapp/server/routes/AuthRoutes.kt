package com.application.bibleapp.server.routes

import io.ktor.server.routing.Route

/**
 * TODO: register the auth/account endpoints here (e.g. register, login, refresh)
 * once their request/response contract is designed. See docs/API_Integration_Guide.pdf.
 */
fun Route.authRoutes() {
}
